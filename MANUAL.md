# GAME ENGINE EDITOR - ПОЛНОЕ РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ

## 🎮 ДОБРО ПОЖАЛОВАТЬ В GAME ENGINE EDITOR v1.0

Это полнофункциональный редактор для создания 2D и 3D игр с интуитивным интерфейсом.

---

## 📋 СОДЕРЖАНИЕ

1. [Установка](#установка)
2. [Запуск](#запуск)
3. [Основные концепции](#основные-концепции)
4. [Интерфейс](#интерфейс)
5. [Создание проекта](#создание-проекта)
6. [Работа с объектами](#работа-с-объектами)
7. [Редактор скриптов](#редактор-скриптов)
8. [Система ассетов](#система-ассетов)
9. [Экспорт игр](#экспорт-игр)
10. [Примеры](#примеры)

---

## 🔧 Установка

### Требования:
- **Python 3.8+** (проверьте версию: `python --version`)
- **Windows/Mac/Linux**

### Шаг 1: Установка Python
Если Python ещё не установлен:
1. Посетите https://www.python.org/downloads/
2. Загрузите Python 3.10 или выше
3. Установите с опцией "Add Python to PATH"

### Шаг 2: Установка зависимостей
Откройте командную строку/терминал и выполните:

```bash
# Перейдите в папку проекта
cd путь/к/game_engine

# Установите требуемые пакеты
pip install -r requirements.txt
```

### Шаг 3: Проверка установки
```bash
python check_installation.py
```

Вы должны увидеть сообщение: **✓ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ!**

---

## 🚀 Запуск

### Способ 1: Через Python (Рекомендуется)
```bash
python run_engine.py
```

### Способ 2: Быстрый старт
```bash
python QUICKSTART.py
```

### Способ 3: На Windows (двойной клик)
Дважды щелкните по файлу `run_engine.py`

---

## 💡 Основные концепции

### GameObject (Игровой объект)
Объект в сцене - может быть:
- **Спрайт 2D** - плоская графика
- **Куб/Сфера 3D** - объёмные фигуры
- **Свет** - источник освещения
- **Камера** - точка обзора
- **Пустой объект** - контейнер для группировки

### Transform (Трансформация)
Положение, ориентация и размер объекта:
- **Position** (Позиция): X, Y, Z координаты
- **Rotation** (Ротация): углы поворота в градусах
- **Scale** (Масштаб): размер (1.0 = нормальный)

### Scene (Сцена)
Контейнер для объектов:
- Содержит все объекты уровня
- Имеет одну активную камеру
- Может быть 2D или 3D

### Project (Проект)
Контейнер для сцен:
- Содержит одну или несколько сцен
- Включает все ассеты и скрипты
- Сохраняется в формате `.project`

### Script (Скрипт)
Python код для логики:
- Определяет поведение объектов
- Имеет методы: `on_start()`, `on_update()`, `on_destroy()`
- Сохраняется как `.py` файл

---

## 🖥️ Интерфейс

### Главное окно:

```
┌─────────────────────────────────────────────────────────┐
│ Меню: Файл | Редактирование | Вид | Экспорт | Помощь   │
├──────────────┬──────────────────────┬──────────────────┤
│              │                      │                  │
│   ЛЕВАЯ      │     ЦЕНТР            │    ПРАВАЯ        │
│   ПАНЕЛЬ     │   (Редактор сцен)    │    ПАНЕЛЬ        │
│              │                      │                  │
│ • Иерархия   │   [ХОЛСТ]            │ • Свойства       │
│ • Ассеты     │   Zoom: мышь         │ • Трансформ.     │
│              │   Pan: Пробел+мышь   │ • Освещение      │
│              │                      │ • Шаблоны        │
│              ├──────────────────────┤                  │
│              │                      │                  │
│              │ (Редактор скриптов)  │                  │
│              │                      │                  │
└──────────────┴──────────────────────┴──────────────────┘
│ Статусная строка: сообщения о действиях                │
└─────────────────────────────────────────────────────────┘
```

### Левая панель:
- **Иерархия** - список всех объектов сцены
- **Ассеты** - встроенные ресурсы (спрайты, модели, звуки)
- Нажмите на объект для выбора

### Центральная часть:
- **Верхняя половина** - визуальный редактор сцен
- **Нижняя половина** - редактор скриптов Python
- Перетащите разделитель для изменения размера

### Правая панель:
- **Вкладка "Свойства"** - редактирование выбранного объекта
- **Вкладка "Освещение"** - настройки света на сцене
- **Вкладка "Шаблоны"** - готовые примеры кода

---

## 📝 Создание проекта

### Первый проект (автоматический):
При запуске создаётся проект "Default Game" со стартовой сценой.

### Новый проект вручную:

1. **Меню** → **Файл** → **Новый проект**
2. **Заполните данные:**
   - Имя проекта (например: "My Game")
   - Папка для сохранения
   - Режим: 2D, 3D или Hybrid
3. **Нажмите "Создать"**

Проект сохранится в виде файла `название.project`

---

## 🎨 Работа с объектами

### Добавление объекта:

1. **Меню** → **Редактирование**
2. Выберите тип:
   - **Добавить спрайт (2D)** - для 2D игр
   - **Добавить куб (3D)** - кубический объект
   - **Добавить свет** - источник освещения
   - **Добавить камеру** - для просмотра

### Выбор и перемещение:

1. **Клик** на объект на холсте
2. **Красная рамка** показывает выбранный объект
3. **Drag-and-drop** для перемещения (левая кнопка мыши)
4. **Колёсико мыши** для масштабирования

### Редактирование свойств:

1. Выберите объект
2. **Правая панель** обновится с его данными
3. **Введите значения:**
   - **Позиция** - координаты X, Y, Z
   - **Ротация** - углы в градусах
   - **Масштаб** - размер (1.0 = нормальный)
4. **Изменения видны в реальном времени**

### Удаление объекта:

1. Выберите объект
2. Нажмите **Delete** или меню **Редактирование** → **Удалить объект**

### Примеры позиций для 2D:
```
Верхний левый угол:    X = -5,  Y = 5
Центр экрана:          X = 0,   Y = 0
Нижний правый угол:    X = 5,   Y = -5
```

---

## 💻 Редактор скриптов

### Открытие редактора:
В центральной части окна, нижняя половина.

### Создание скрипта:

1. **Нажмите "Новый скрипт"**
2. **Выберите шаблон** из вкладки "Шаблоны" или напишите с нуля
3. **Введите код** на Python
4. **Нажмите "Сохранить"** (Ctrl+S)

### Структура скрипта:

```python
class GameScript:
    def __init__(self, game_object):
        # Инициализация
        self.game_object = game_object
        self.speed = 5.0
    
    def on_start(self):
        # Вызывается один раз при запуске
        print(f"{self.game_object.name} создан!")
    
    def on_update(self, delta_time):
        # Вызывается каждый кадр
        # delta_time - время с предыдущего кадра
        self.game_object.transform.position.x += self.speed * delta_time
    
    def on_destroy(self):
        # Вызывается при удалении объекта
        print(f"{self.game_object.name} удалён!")
```

### Доступные шаблоны:

1. **Пустой скрипт** - базовая структура
2. **Движение персонажа** - управление движением
3. **Система здоровья** - здоровье и урон
4. **Система оружия** - оружие и стрельба
5. **AI враг** - поведение врага
6. **Система сохранений** - сохранение игры

### Отладка скриптов:

1. **Нажмите "Запустить тест"** для проверки синтаксиса
2. **Результат** выведется в консоль внизу
3. **Используйте print()** для отладки:
   ```python
   print(f"Позиция: {self.game_object.transform.position.x}")
   ```

### Назначение скрипта объекту:

1. Выберите объект
2. **Правая панель** → **Свойства**
3. Найдите поле **"Script"** (если видно)
4. Введите имя файла скрипта (например: `player_movement.py`)

---

## 📦 Система ассетов

### Встроенные ассеты (готовые):

**Спрайты 2D:**
- character, background, button, tile, particle

**3D Модели:**
- cube, sphere, pyramid, cylinder, plane

**Звуки:**
- jump, select, powerup, explosion, background_music

**Эффекты:**
- smoke, fire, water_splash, electric, bloom

**Текстуры:**
- metal, wood, stone, glass, plastic, cloth, rust, ice

### Использование ассетов:

1. **Откройте левую панель** → **Ассеты**
2. **Выберите вкладку** (Спрайты, 3D Модели и т.д.)
3. **Нажмите на нужный ассет**
4. Статус-бар покажет выбранный ассет

### Импортирование ассетов:

1. **Нажмите "Импортировать ассет"**
2. **Выберите файл** (PNG, JPG, MP3, WAV, OBJ, FBX)
3. **Ассет будет скопирован** в папку `game_engine/assets/`

### Поддерживаемые форматы:

- **Изображения:** PNG, JPG, BMP, GIF
- **Звуки:** MP3, WAV, OGG, FLAC
- **Модели:** OBJ, FBX, GLTF
- **Другое:** любые бинарные файлы

---

## 🎬 Экспорт игр

### Доступные форматы:

#### 1. Standalone (для ПК)
- Создаёт исполняемый файл
- Поддерживает Windows, Mac, Linux
- Полная функциональность

#### 2. Web (для браузера)
- HTML5 / WebGL
- Работает в любом браузере
- Меньший размер файлов

#### 3. Mobile (для смартфонов)
- iOS / Android приложение
- Оптимизировано для сенсорного ввода
- Меньший расход батареи

### Процесс экспорта:

1. **Убедитесь, что проект сохранён** (Ctrl+S)
2. **Меню** → **Экспорт**
3. **Выберите формат** (Standalone / Web / Mobile)
4. **Выберите папку** для экспорта
5. **Приложение создаст структуру**

### Содержимое экспорта:

```
game_export/
├── launcher.py       # Главный файл запуска
├── scenes/           # Все сцены (JSON)
├── scripts/          # Все скрипты (Python)
└── assets/           # Графика, звуки, модели
```

### Запуск экспортированной игры:

**Windows/Mac/Linux:**
```bash
python launcher.py
```

**На веб-сервере:**
1. Загрузите содержимое на веб-сервер
2. Откройте index.html в браузере

---

## 📚 Примеры

### Пример 1: Простая 2D сцена

```python
# скрипт: simple_sprite.py
class SimpleSprite:
    def __init__(self, game_object):
        self.game_object = game_object
    
    def on_start(self):
        print(f"Создан спрайт: {self.game_object.name}")
    
    def on_update(self, delta_time):
        # Вращение
        self.game_object.transform.rotation.z += 45 * delta_time
```

### Пример 2: Движущийся персонаж

```python
# скрипт: player_movement.py
class PlayerMovement:
    def __init__(self, game_object):
        self.game_object = game_object
        self.speed = 10.0
    
    def on_update(self, delta_time):
        # Простое движение вправо
        self.game_object.transform.position.x += self.speed * delta_time
        
        # Если вышли за край, вернуться
        if self.game_object.transform.position.x > 20:
            self.game_object.transform.position.x = -20
```

### Пример 3: Система счётчика

```python
# скрипт: score_system.py
class ScoreSystem:
    def __init__(self):
        self.score = 0
    
    def add_points(self, points):
        self.score += points
        print(f"Очки: {self.score}")
    
    def reset(self):
        self.score = 0
```

### Больше примеров:
Откройте файл `game_engine/scripts/examples.py` - там 10+ готовых примеров!

---

## 🎮 Создание первой простой игры

### Этап 1: Подготовка (5 минут)
1. Создайте новый проект (Файл → Новый проект)
2. Выберите режим **2D**

### Этап 2: Создание сцены (10 минут)
1. **Добавьте спрайт** (Редактирование → Добавить спрайт)
2. Назовите его "Player"
3. Разместите в центре (X=0, Y=0)

### Этап 3: Написание скрипта (15 минут)
1. Откройте редактор скриптов (нижний центр)
2. Создайте новый скрипт
3. Используйте шаблон "Движение персонажа"
4. Модифицируйте для своих нужд
5. Сохраните (Ctrl+S)

### Этап 4: Добавление враги (10 минут)
1. Добавьте ещё один спрайт для врага
2. Разместите его в стороне (X=5, Y=0)
3. Добавьте скрипт из "Система здоровья"

### Этап 5: Экспорт (5 минут)
1. Сохраните проект (Ctrl+S)
2. Меню → Экспорт → выберите формат
3. Выберите папку для сохранения
4. Ваша первая игра готова!

---

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **Ctrl+N** | Новый проект |
| **Ctrl+O** | Открыть проект |
| **Ctrl+S** | Сохранить |
| **Ctrl+Z** | Отмена |
| **Ctrl+Y** | Повтор |
| **Delete** | Удалить выбранный объект |
| **Ctrl+D** | Дублировать объект |
| **F5** | Тест скрипта |
| **Колёсико мыши** | Масштабирование сцены |
| **Пробел + мышь** | Панорамирование сцены |

---

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'PyQt5'"

**Решение:**
```bash
pip install PyQt5
```

### Проблема: Приложение не запускается

**Проверьте:**
1. Python 3.8+ установлен: `python --version`
2. PyQt5 установлен: `pip install PyQt5`
3. Все файлы на месте: `python check_installation.py`

### Проблема: Объекты не видны на холсте

**Решение:**
1. Используйте колёсико мыши для масштабирования
2. Проверьте позицию объекта (может быть вне видимой области)
3. Добавьте объект заново

### Проблема: Скрипт не работает

**Проверьте:**
1. Синтаксис: нажмите "Запустить тест"
2. Имя скрипта: убедитесь, что правильно указано
3. Консоль: смотрите ошибки в консоли внизу

### Проблема: Не получается сохранить скрипт

**Решение:**
1. Убедитесь, что указано имя скрипта
2. Используйте только латинские буквы и подчёркивания
3. Не используйте пробелы в имени

---

## 💬 Поддержка

### Документация:
- Встроенная справка: Меню → Помощь → О программе
- Подробное руководство: `game_engine/documentation.py`
- Примеры кода: `game_engine/scripts/examples.py`

### Для отладки используйте:
```bash
python check_installation.py
```

---

## 📞 Больше информации

- **README.md** - основная информация о проекте
- **documentation.py** - подробное руководство
- **requirements.txt** - список зависимостей
- **engine.config** - конфигурация приложения

---

## 🎯 Советы и трюки

1. **Регулярно сохраняйте** - используйте Ctrl+S часто
2. **Используйте понятные имена** - "Player" вместо "Sprite1"
3. **Группируйте объекты** - используйте Empty объекты
4. **Тестируйте рано** - проверяйте скрипты часто
5. **Читайте примеры** - они содержат полезный код
6. **Документируйте код** - комментируйте сложные части

---

## ✅ Заключение

Вы готовы начать создавать игры! Следуйте этому руководству пошагово, 
и вы быстро научитесь использовать Game Engine Editor.

**Удачи в разработке ваших игр! 🎮✨**

---

**Версия:** 1.0  
**Дата обновления:** 2024  
**Статус:** ✓ Полностью функционален
